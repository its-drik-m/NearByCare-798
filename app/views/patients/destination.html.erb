<div class="pages-margin-top map-container" style="width: 100vw; height: 100vh;"
  data-controller="destination"
  data-destination-markers-value="<%= @marker.to_json %>"
  data-destination-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>
<div class="d-flex align-items-center btn-container">
  <%= link_to "Go to patient's place ?", patient_address_path(@path), class: "btn btn-custom " %>
</div>
