<div class="container card rounded- mt-43 " style="width: 600px">
  <div class="row">
    <div class="col">
      <div class="form p-3">
        <%= render 'form', patient: @patient %>
      </div>
        <%# <div class="form p-3">
          <%= link_to "Edit your bookings", edit_booking_path(@booking.find_by_patient_id(@patient.id)), class: "btn-submit-carer" %>
        <%# </div> %>
    </div>
  </div>
</div>
