<div class="col-4">
  <div class="card">
    <%= simple_form_for([@carer, @booking]) do |f| %>
      <%= f.submit 'Confirm', class: 'btn btn-green' %>
      <%= button_to "Reject", carer_path,
      data: { turbo_method: "delete", turbo_confirm: "Are you sure?" } %>
    <% end %>
  </div>
</div>
